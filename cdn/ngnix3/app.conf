server {

    listen 8083 default_server;

    root /var/www/html;
    index index.html;
    allow all;

    location / {
         try_files $uri $uri/ $uri.html =404;
         add_header x-server "3";
    }
}
